/// @description Insert description here
// You can write your code in this editor

if (dLife < life) dLife++;
else instance_destroy();
alpha += dAlpha;
angle += dAngle;
speed += dSpeed;
scale += dScale;